form someName {
    "Some question" money: float
    "Some other question" newvar: int
    "another question" cool: bool = 1 + 2
    if(2 <= 1) {
        "Some indented question" newvar: bool
    } else if(1 - 2 >= 0) {
        "some" newvar: bool
    } else {
        "seome asldjfalsdkf a" newvar: int
    }
}